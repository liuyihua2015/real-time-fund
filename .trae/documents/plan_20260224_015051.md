**上次修改内容总结**

1.  **修复 API 404 及跨域问题**
    *   **详情接口**: 将失效的 `FundVarietieVali` 替换为 `FundMNewApi/FundMNBasicInformation`。
    *   **持仓接口**: 将失效的 `FundPositionStockList` 替换为 `FundMNewApi/FundMNInverstPosition`。
    *   **历史净值**: 增加通过 `pingzhongdata` 脚本注入方式获取历史数据，解决跨域问题。
    *   **数据适配**: 更新了数据解析逻辑以适配新接口的字段变化（如 `Datas` 替代 `Expansion`，`GGPNAME` 替代 `GPPNAME`）。

2.  **适配 GitHub Pages 静态部署 (路由改造)**
    *   **移除动态路由**: 删除了 `app/fund/[code]` 目录（GitHub Pages 不支持 Next.js 的动态路由路径）。
    *   **改为查询参数**: 将基金详情页迁移至 `app/fund/page.jsx`，并改为使用 URL 查询参数（如 `/fund?code=002112`）。
    *   **组件迁移**: 将相关组件（`FundChart`, `FundCardDetailClient` 等）移动到 `app/fund/` 目录下。
    *   **链接更新**: 修改了首页和列表页的跳转链接，适配新的路由格式。

此修改解决了部署后刷新页面 404 以及接口请求失败的问题。